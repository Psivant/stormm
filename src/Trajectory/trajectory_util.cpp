#include <string>
#include "copyright.h"
#include "Synthesis/systemcache.h"
#include "coordinate_copy.h"
#include "trajectory_util.h"

namespace stormm {
namespace trajectory {

using synthesis::SystemCache;

//-------------------------------------------------------------------------------------------------
void writeSynthesisFrames(const PhaseSpaceSynthesis &poly_ps, Condensate *staging_zone,
                          const Hybrid<int2> &system_pairs, const SynthesisCacheMap &scmap,
                          const CoordinateFileKind crd_format, const double current_time,
                          const TrajectoryKind traj_kind, const HybridTargetLevel tier,
                          const GpuDetails &gpu, const BrokenAsciiCode recovery) {

  // Determine the printing style based on the trajectory type
  PrintSituation general_expct;
  switch (crd_format) {
  case CoordinateFileKind::AMBER_CRD:
  case CoordinateFileKind::AMBER_NETCDF:
    general_expct = PrintSituation::APPEND;
    break;
  case CoordinateFileKind::AMBER_INPCRD:
  case CoordinateFileKind::AMBER_ASCII_RST:
  case CoordinateFileKind::AMBER_NETCDF_RST:
  case CoordinateFileKind::SDF:
  case CoordinateFileKind::PDB:
    general_expct = PrintSituation::OVERWRITE;
    break;
  case CoordinateFileKind::UNKNOWN:
    break;
  }
  const DataFormat file_genus = getTrajectoryFormat(crd_format);

  // Stage the data as real-valued numbers.  This can be done regardless of the memory tier at
  // which the GPU is storing resources.
  coordCopy(staging_zone, poly_ps, traj_kind, system_pairs, HybridTargetLevel::HOST, tier, gpu);

  // The system data is now present on CPU resources.  Iterate over each system and write
  // trajectory files.
  const int nsys = system_pairs.size();
  const CondensateWriter cdnsr = staging_zone->data();
  const SystemCache *sc = scmap.getCachePointer();
  const int xfrm_stride = roundUp(9, warp_size_int);
  for (int i = 0; i < nsys; i++) {

    // Take the coordinates in the index indicated by the "y" member of the tuple.  This is the
    // destination slot where they ended up in the Condensate.  Get the file name based on the
    // system index in the "x" member of the tuple, the slot where in came from in the
    // PhaseSpaceSynthesis which is tied to the original cache of input files.
    const int2 ipair = system_pairs.readHost(i);
    const int orig_idx = ipair.x;
    const int dest_idx = ipair.y;
    const int sysc_idx = scmap.getSystemCacheIndex(i);
    const std::string i_filename = sc->getTrajectoryName(sysc_idx);
    std::ofstream foutp;
    const bool fi_exists = (getDrivePathType(i_filename) == DrivePathType::FILE);
    foutp = openOutputFile(i_filename, general_expct, "Open an output file for writing "
                           "coordinate synthesis contents", file_genus);
    char buffer[128];
    switch (crd_format) {
    case CoordinateFileKind::AMBER_CRD:

      // Initialize the trajectory if this is the first time printing it
      if (fi_exists == false || general_expct == PrintSituation::OVERWRITE ||
          general_expct == PrintSituation::OPEN_NEW) {
        snprintf(buffer, 128, "Generated by STORMM\n");
        foutp.write(buffer, strlen(buffer));
      }
      break;
    case CoordinateFileKind::AMBER_NETCDF:
      break;
    case CoordinateFileKind::AMBER_INPCRD:
    case CoordinateFileKind::AMBER_ASCII_RST:
    case CoordinateFileKind::AMBER_NETCDF_RST:
    case CoordinateFileKind::SDF:
    case CoordinateFileKind::PDB:
      break;
    case CoordinateFileKind::UNKNOWN:
      rtErr("Printing request for unknown file type when writing trajectory " + i_filename +
            ", system index " + std::to_string(orig_idx) + " of the synthesis with " +
            std::to_string(poly_ps.getAtomCount(orig_idx)) + " atoms.", "writeSynthesisFrames");
    }
    const int iatom_start = cdnsr.atom_starts[dest_idx];
    const int iatom_count = cdnsr.atom_counts[dest_idx];
    switch (cdnsr.mode) {
    case PrecisionModel::DOUBLE:
      writeFrame<double>(&foutp, i_filename, crd_format, iatom_count, &cdnsr.xcrd[iatom_start],
                         &cdnsr.ycrd[iatom_start], &cdnsr.zcrd[iatom_start], nullptr, nullptr,
                         nullptr, poly_ps.getUnitCellType(), &cdnsr.boxdims[xfrm_stride * i],
                         recovery);
      break;
    case PrecisionModel::SINGLE:
      writeFrame<float>(&foutp, i_filename, crd_format, iatom_count, &cdnsr.xcrd_sp[iatom_start],
                        &cdnsr.ycrd_sp[iatom_start], &cdnsr.zcrd_sp[iatom_start], nullptr,
                        nullptr, nullptr, poly_ps.getUnitCellType(),
                        &cdnsr.boxdims[xfrm_stride * i], recovery);
      break;
    }
  }
}

} // namespace trajectory
} // namespace stormm
